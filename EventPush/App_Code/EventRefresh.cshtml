@helper Action(string url, IHtmlString content, IEnumerable<string> eventTypes, System.Web.Routing.RouteValueDictionary parameters)
{
    
    <div data-refresh-action="@url"        >
        @content
    </div>
}